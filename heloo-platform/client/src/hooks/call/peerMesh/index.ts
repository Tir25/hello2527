/**
 * Peer Mesh Module Index
 * 
 * Re-exports the usePeerMesh hook and related types.
 * @module hooks/call/peerMesh
 */

export { usePeerMesh } from './usePeerMesh'
export type { PeerConnection, UsePeerMeshOptions, UsePeerMeshReturn } from './types'
export { DEFAULT_ICE_SERVERS } from './config'
