export default {
    plugins: {
        '@tailwindcss/postcss': {},
        // Note: Tailwind CSS v4 includes autoprefixer by default
        // autoprefixer is not needed here but kept for explicit control if needed
    },
}
